Simple Tetris game created with create-react-app using flowtype. In development.
Clone the repository and install the dependencies with

```
> npm install
```
and run the app with
```
> npm start
```
